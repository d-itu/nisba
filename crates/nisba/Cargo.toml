[package]
name = "nisba"
version = "0.1.0"
edition = "2024"

[dependencies]
static_assertions = "1.1.0"
thiserror = { version = "2.0.18", default-features = false }

[features]
default = ["alloc"]
alloc = []
